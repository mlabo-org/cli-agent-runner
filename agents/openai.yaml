display_name: CLI Agent Runner
short_description: Run scoped CLI workers with an IAB Live Console.
default_prompt: Use CLI Agent Runner to automatically start and open its Codex IAB Live Console, launch a scoped Grok or custom CLI worker, stream activity, and record process, artifact-acceptance, and result-contract state.
