display_name: CLI Agent Runner
short_description: Run scoped CLI workers with a default-on IAB Live Console.
default_prompt: Use CLI Agent Runner to open its IAB Live Console before project work, keep it ready, then launch a scoped Grok or CLI worker and record execution, artifact-acceptance, and result-contract state. Disable it only on explicit request.
