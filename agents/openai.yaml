display_name: CLI Agent Runner
short_description: Run CLI workers, brokered local descendants, or parallel leaf jobs with a default-on IAB Live Console.
default_prompt: Open IAB Live Console first; use run, explicit local_orchestrator delegation, or orchestrate leaf jobs through the shared viewer, then stop after success.
