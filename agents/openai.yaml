display_name: CLI Agent Runner
short_description: Run one CLI worker or parallel independent leaf jobs with a default-on IAB Live Console.
default_prompt: Open its IAB Live Console first, keep it across user-confirmation pauses, restore it before resuming; use run for one worker or orchestrate --jobs-file for independent leaf jobs, sharing the console and stopping after success.
