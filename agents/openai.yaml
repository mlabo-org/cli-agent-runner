display_name: CLI Agent Runner
short_description: Run scoped CLI workers with a default-on IAB Live Console.
default_prompt: Use CLI Agent Runner: open its IAB Live Console first, keep it across user-confirmation pauses, restore it before resuming, launch the scoped CLI worker, and stop after a minimal completed result on exit zero.
