display_name: CLI Agent Runner
short_description: Launch scoped Codex, Claude, Grok, or configured CLI workers.
default_prompt: Use CLI Agent Runner to launch a scoped built-in or JSON-configured CLI worker, capture its result, and record process-runner-result state.
