display_name: CLI Agent Runner
short_description: Launch scoped Codex CLI workers and normalize their process results.
default_prompt: Use CLI Agent Runner to launch a scoped Codex CLI worker with codex exec, capture its result, and record process-runner-result state.
